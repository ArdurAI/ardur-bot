/** Product palette for Expo. Prefer these over hardcoded Ardur Bot hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@ardurbot/ui-tokens";
